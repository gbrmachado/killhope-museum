<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="uk.ac.dur.group1.killhope_museum" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:name="uk.ac.dur.group1.killhope_museum.KillhopeApplication">
        <activity
            android:name=".activity.SplashScreenActivity"
            android:label="@string/title_activity_splash_screen" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.MainMenuActivity"
            android:label="@string/title_activity_main_menu" >
        </activity>
        <activity
            android:name=".activity.QRCodeActivity"
            android:label="@string/title_activity_qrcode" >
        </activity>
        <activity
            android:name=".activity.MapsActivity"
            android:label="@string/title_activity_maps" >
        </activity>
        <activity
            android:name=".activity.ExhibitionsActivity"
            android:label="@string/title_activity_exhibitions" >
        </activity>
        <activity
            android:name=".activity.RockListActivity"
            android:label="@string/title_activity_rock_list" >
        </activity>
        <activity
            android:name=".activity.RockDisplayActivity"
            android:label="@string/title_activity_rock_display" >
        </activity>
        //required to detect and not destory the cached image.
        <activity
            android:name=".activity.FullScreenImageActivity"
            android:label="@string/title_activity_full_screen_image"
            android:configChanges="orientation|screenSize"
            android:noHistory="true">
        </activity>

        <activity android:name=".activity.quiz_homeActivity"
            android:label="quiz home"></activity>

        <activity android:name=".activity.quiz_processActivity"
            android:label="quiz Processing"></activity>

        <activity android:name=".activity.quiz_resultActivity"
            android:label="quiz result"></activity>

    </application>

</manifest>
